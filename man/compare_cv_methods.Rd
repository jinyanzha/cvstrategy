% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_table.R
\name{compare_cv_methods}
\alias{compare_cv_methods}
\title{Compare four CV strategies and return a summary table}
\usage{
compare_cv_methods(
  X,
  y,
  lambda_path = NULL,
  K = 10,
  foldid = NULL,
  seed = 123,
  n_lambda_coarse = 20,
  n_lambda_fine = 40,
  threshold = 0.5
)
}
\arguments{
\item{X}{Numeric design matrix (n x p), no intercept column.}

\item{y}{Numeric binary response vector in {0,1}.}

\item{lambda_path}{Optional numeric vector of lambdas.}

\item{K}{Number of folds if foldid is NULL.}

\item{foldid}{Optional integer vector in 1:K.}

\item{seed}{Random seed used only when foldid is NULL.}

\item{n_lambda_coarse}{Number of coarse lambdas for adaptive CV.}

\item{n_lambda_fine}{Number of fine lambdas for adaptive CV.}

\item{threshold}{Classification threshold for accuracy.}
}
\value{
A data.frame comparing four methods.
}
\description{
Compare four CV strategies and return a summary table
}
\examples{
if (requireNamespace("mlbench", quietly = TRUE)) {
  data(Sonar, package = "mlbench")
  X <- as.matrix(Sonar[, 1:60])
  y <- ifelse(Sonar$Class == "M", 1, 0)

  tab <- compare_cv_methods(X, y, K = 5, seed = 1)
  print(tab)
}
}
